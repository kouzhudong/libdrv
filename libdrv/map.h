/*
这里的map非stl::map，但是实现stl::map类似的功能。

这里的map是对Rtl*GenericTable*系列函数的封装，用模板实现泛型编程。

用此文要理解模板的意义和编译器的实现。

注意：对于函数重载，模板函数的调试一个有用的命令是bm.
*/

#pragma once

#include "pch.h"

class map
{

};
